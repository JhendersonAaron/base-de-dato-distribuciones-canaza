name: 'canaza_bi'
version: '1.0.0'
config-version: 2

profile: 'canaza_bi'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

log-path: "logs"
target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  canaza_bi:
    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: table
      +schema: marts